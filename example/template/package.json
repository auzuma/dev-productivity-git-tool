{
  "name": "{{ packageName }}",
  "version": "{{ version }}",
  "description": "{{ projectDescription }}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "keywords": [{{ keywords }}],
  "author": "{{ author }}",
  "license": "{{ license }}"
}
